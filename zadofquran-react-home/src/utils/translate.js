import en from "../translations/enTrans.json";
import ar from "../translations/arTrans.json";
export default function languages(lang, key) {
    //   if (lang === "en") {
    //     return enTrans[key];
    //   } else if (lang === "ar") {
    //     return arTrans[key];
    //   }
}
export const translations = {
    en,
    ar,
};
